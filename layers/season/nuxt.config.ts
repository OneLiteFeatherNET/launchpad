// Layer: season — the site's calendar-driven costume (Halloween): which season
// is in effect, its assets and its decoration. Colours come from
// assets/css/seasons.css; app.vue and the layout wire the rest in.
export default defineNuxtConfig({})
